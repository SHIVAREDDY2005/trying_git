pc  ----->staging------->git

M  == modified
U  == untracked
A  == Added

git --version
git config --global --list
git init
git branch -M main
        (or)
git init -b main                -->to specify branch
git status                      -->status
git add filename                -->staging
git add .                       -->add all files to staging
git commit -m "comment"         -->local file
git commit -a -m "comment"      --> no need of staging 
git log                         --> to check all commits
git diff                        -->(diff between working directory and local)
git diff --staged               -->(diff between staging and local)
git ls-files                    -->files in git
git rm -cached filename         -->remove file from git
git clone (http link of github project)




 ssh-keygen -o              ->create ssh
 (it generate .ssh folder in that file)cat id_ed25519.pub(contain key)